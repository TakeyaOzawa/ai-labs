---
inclusion: auto
description: specのdesign.md作成時のフォーマット。IEEE 1016:2009準拠のセクション構成とテンプレートを定義する
---

# Design フォーマットガイド

specのdesign.mdを作成する際は、本ガイドのフォーマットに従ってください。
本フォーマットはIEEE 1016:2009（SDD）の構造を、Kiro SDD向けに簡略化したものです。

## 準拠規格

- IEEE 1016:2009 — Software Design Descriptions
- ISO/IEC/IEEE 42010:2011 — Architecture description（アーキテクチャビューポイントの考え方を参照）
- ISO/IEC 14764:2022 — Software engineering — Software life cycle processes — Maintenance
- 簡略化の方針: IEEE 1016のDesign Viewpoint概念を、実用的なセクション構成に変換。

## ISO/IEC 14764 保守分類との対応

design.mdの構成は、requirements.mdの `spec_type` に応じて変更種別ごとの追加セクションを持つ。
ISO/IEC 14764の保守分類との対応はグローバルsteering `requirements-format-guide.md` を参照。

設計書では特に以下の観点で保守分類を意識する:

| ISO/IEC 14764 分類                  | 設計書での重点セクション                         |
| ----------------------------------- | ------------------------------------------------ |
| Corrective（是正: fix）             | 修正方針、副作用の回避策                         |
| Adaptive（適応: feat）              | アーキテクチャ、コンポーネント設計、データモデル |
| Perfective（完全化: perf/refactor） | ボトルネック分析、変更前後の対比、互換性の確保   |
| Preventive（予防: refactor）        | 変更前後の対比、互換性の確保                     |

## front-matter 仕様

requirements.mdと同一のfront-matterを使用する。
フィールド定義はグローバルsteering `requirements-format-guide.md` を参照。

## テンプレート

```markdown
---
spec_type: { feat / fix / refactor / perf / docs }
feature_name: { feature-name }
status: active
created_at: { YYYY-MM-DD }
created_by: { 作成者 }
updated_at: { YYYY-MM-DD }
updated_by: { 更新者 }
related_domain:
  - { domain/file }
---

# 設計書

<!-- IEEE 1016 Section 1: Introduction -->

## 1. はじめに

### 1.1 目的

<!-- この設計書の目的。requirements.mdの要件をどのように実現するかを記述 -->

### 1.2 設計方針

<!--
  全体的な設計方針・アプローチの概要。
  なぜこの設計アプローチを選んだのか、検討した代替案があればその比較と
  採用理由を記述する（IEEE 1016 Design Rationale Viewpoint）。
  意思決定の根拠を残すことで、将来の設計変更時に「なぜこうなったか」を追跡できる。
-->

### 1.3 参照文書

<!-- 対応するrequirements.md、関連するドメイン知識、外部仕様書 -->

<!-- IEEE 1016 Context Viewpoint -->

## 2. アーキテクチャ

### 2.1 システム構成

<!--
  - システム構成図（mermaid推奨）
  - 既存システムとの統合ポイント
-->

### 2.2 レイヤー構成

<!--
  - プレゼンテーション層
  - アプリケーション層（Controller / Service）
  - ドメイン層（Model / Enum）
  - インフラストラクチャ層（DB / 外部API）
-->

<!-- IEEE 1016 Composition Viewpoint -->

## 3. コンポーネント設計

### 3.1 コンポーネント一覧

| コンポーネント | ファイルパス | 責務         | 新規/変更   |
| -------------- | ------------ | ------------ | ----------- |
| {クラス名}     | {app/...}    | {責務の説明} | 新規 / 変更 |

### 3.2 コンポーネント間インターフェース

<!-- コンポーネント間の依存関係とインターフェース定義 -->

### 3.3 主要クラスの設計

<!--
  各コンポーネントの詳細設計:
  - メソッドシグネチャ
  - 入出力
  - ビジネスロジックの概要
-->

<!-- IEEE 1016 Information Viewpoint -->

## 4. データモデル

### 4.1 テーブル設計

<!-- 新規テーブルの場合 -->

| カラム名   | 型   | NULL     | デフォルト     | コメント   |
| ---------- | ---- | -------- | -------------- | ---------- |
| {カラム名} | {型} | {YES/NO} | {デフォルト値} | {コメント} |

<!-- 既存テーブルへの変更の場合 -->

| 変更種別 | カラム名   | 型   | 変更内容   |
| -------- | ---------- | ---- | ---------- |
| 追加     | {カラム名} | {型} | {変更内容} |

### 4.2 ER図

<!-- mermaid erDiagram推奨 -->

### 4.3 マイグレーション方針

<!-- マイグレーションファイルの方針、実行順序、ロールバック考慮 -->

<!-- IEEE 1016 Interaction Viewpoint -->

## 5. 処理フロー

### 5.1 {ユースケース名}

<!-- シーケンス図（mermaid推奨）で主要な処理フローを記述 -->

### 5.2 エラーハンドリング

<!-- エラー発生時の処理フロー -->

### 5.3 バッチ処理

<!-- バッチ処理がある場合のフロー（該当しない場合は省略可） -->

<!-- IEEE 1016 Dependency Viewpoint -->

## 6. 外部インターフェース

### 6.1 API

<!-- 外部APIとの連携がある場合 -->

### 6.2 UI

<!-- 管理画面のUI設計（Laravel-Admin Grid/Form/Show の構成） -->

## 7. セキュリティ考慮

<!--
  この変更に関連するセキュリティ上の考慮事項を記述する。
  該当しない場合は「該当なし」と記載。

  考慮すべき観点:
  - 認証・認可: アクセス制御の変更があるか
  - 入力検証: ユーザー入力のバリデーション・サニタイズ
  - データ保護: 個人情報・機密情報の取り扱い
  - SQLインジェクション・XSS等の脆弱性対策
  - Mass Assignment保護
-->

## 8. テスト戦略

<!-- ワークスペースにテストガイドラインがある場合はそのルールに従う -->

| テスト種別 | 対象             | 方針         | 対応要件   |
| ---------- | ---------------- | ------------ | ---------- |
| Feature    | {コントローラ名} | {テスト方針} | REQ-{番号} |
| Unit       | {サービス名}     | {テスト方針} | REQ-{番号} |

## 9. 要件トレーサビリティ

<!-- IEEE 1016 / 29148 Requirements Traceability Matrix -->

| 要件ID     | 受入基準 | 設計コンポーネント | テスト           |
| ---------- | -------- | ------------------ | ---------------- |
| REQ-{番号} | {番号}   | {クラス名}         | {テストクラス名} |

## 10. 影響範囲

### 10.1 変更対象ファイル

<!-- 変更・作成するファイルの一覧 -->

### 10.2 既存機能への影響

<!--
  requirements.mdの影響範囲分析を受けて、設計レベルで具体的な対処方針を記述する。

  #### 影響分析

  | 影響を受ける機能 | 影響の種類 | 対処方針 | 確認方法 |
  | --- | --- | --- | --- |
  | {機能名} | 直接 / 間接 | {回避策・修正方針} | {テスト名 / 手動確認手順} |

  #### 後方互換性の確保

  - API互換性: {既存APIの呼び出し元への影響と対処}
  - データ互換性: {既存データの読み書きへの影響と対処}
  - UI互換性: {既存画面の操作フローへの影響と対処}

  影響がない場合は「既存機能への影響なし」と明記する。
-->

### 10.3 マイグレーション実行時の注意事項

<!-- 本番環境でのマイグレーション実行に関する注意事項（該当する場合） -->

### 10.4 ロールバック戦略

<!--
  問題発生時のロールバック方針を記述する。該当しない場合は「該当なし」と記載。
  - マイグレーションのロールバック手順（down()メソッドの内容）
  - 機能フラグによる切り戻し（使用する場合）
  - 設定変更の巻き戻し手順
  - データ復旧が必要な場合の手順
-->
```

## 変更種別ごとの追加セクション

### fix の場合: セクション2に「修正方針」を追加

```markdown
### 2.3 修正方針

<!-- 根本原因に対するアプローチ、修正箇所の特定、副作用の回避策 -->
```

### refactor の場合: セクション2に「変更前後の対比」を追加

```markdown
### 2.3 変更前後の対比

<!-- 変更前のコード構造・命名 → 変更後のコード構造・命名 -->

### 2.4 互換性の確保

<!-- 後方互換性の維持方法、データ移行手順、ロールバック手順 -->
```

### perf の場合: セクション2に「ボトルネック分析」を追加

```markdown
### 2.3 ボトルネック分析

<!-- 現状のパフォーマンス計測結果、ボトルネックの特定、改善アプローチの比較検討 -->
```

## 記述ルール

- mermaid図を積極的に使用する（アーキテクチャ図、シーケンス図、ER図、ステートマシン図）
- ファイルパスは `app/` からの相対パスで記載する
- 要件トレーサビリティマトリクスで、requirements.mdの要件IDと設計コンポーネントを対応づける

## 影響分析のフロー

デグレ・既存機能への影響は、requirements → design → tasks の3段階で分析する:

```
requirements.md §6 影響範囲
  → 「何に影響するか」を特定（影響を受ける機能、リグレッションリスク、後方互換性）

design.md §10.2 既存機能への影響
  → 「どう対処するか」を設計（回避策、互換性確保の方針、確認方法）

tasks.md フェーズ3
  → 「リグレッションテスト」として実行（影響範囲で特定したリスク箇所を対象に含める）
```

requirements.mdで影響範囲が「なし」と判断された場合でも、design.mdの§10.2で「既存機能への影響なし」と明記すること。
