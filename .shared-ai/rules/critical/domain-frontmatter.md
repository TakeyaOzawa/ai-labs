# Domain Front-matter ルール

`docs/domain/**/*.md` ファイルの編集時に以下を確認し、不足があれば自動的に修正すること。

1. front-matterの `updated_at` が今日の日付（YYYY-MM-DD）に更新されているか
2. front-matterの `updated_by` が適切な値に更新されているか
3. 変更履歴テーブルに今回の変更が記録されているか
